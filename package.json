{
  "name": "per-diem-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "start": "pnpm -r --parallel start",
    "build": "pnpm -r --parallel build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf .next node_modules dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.0.0"
  }
}